#include <stdio.h>
/**
 * main - Print some letters of the alphabet
 * Return: Always 0
 */

int main(void)
{
	char apb;

	for (apb = 'a'; apb <= 'z' && != 'e' 'q'; apb++)
	{
		putchar(apb);
	}
	putchar('\n');
	return (0);
}
